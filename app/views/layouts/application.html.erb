<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : ENV["APP_NAME"] %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : ENV["APP_NAME"] + " is a platform which manages a list of media you can borrow from other users."%>" >
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container">
      <nav class="navbar navbar-default" role="navigation">
          <%= render 'layouts/navigation' %>
      </nav>
      <div class="content">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
